{
  "name": "ina-igar-banco",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Arranque del banco remoto en Railway. Corre el mismo motor que el teléfono (nodo/credito.mjs → mobile/src/core/credito).",
  "scripts": {
    "start": "ROL=banco SKIP_P2P=1 node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON nodo/index.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "zod": "4.5.4"
  }
}

# Landing · sitio y admin del banco

Sitio del proyecto + back office de solicitudes.

Estado vivo y URLs de producción: [`docs/ESTADO.md`](../docs/ESTADO.md).

```bash
npm install   # si aplica
npm start     # sirve index + /admin (PORT)
```

Admin: login por defecto `banco@gmail.com`. Lista solicitudes del banco
(`recibida`, `canal`). En producción apunta a Railway vía `BANCO_URL`.
